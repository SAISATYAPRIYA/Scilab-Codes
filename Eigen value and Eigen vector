clc; clear;  // Clear the command window and all variables

// Step 1: Define the matrix A
a = [2 0 -1; 0 2 0; -1 0 2];

// Step 2: Compute eigenvectors and eigenvalues
// spec(A) returns eigenvalues 
// [v, d] = spec(a)
//   ->v = matrix of eigenvectors (columns are eigenvectors)
//   -> d = diagonal matrix with eigenvalues
[v, d] = spec(a);

// Step 3: Display eigenvalues
// spec(a) gives eigenvalues as a vector
disp("The eigen value is :", spec(a));

// Step 4: Display eigenvectors
// v contains eigenvectors (each column corresponds to one eigenvalue)
disp("The eigen vector is:", v);
